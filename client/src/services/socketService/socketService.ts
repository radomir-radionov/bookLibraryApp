import { io, Socket } from 'socket.io-client';

// const socketService: Socket = io('http://localhost:', {
//   autoConnect: false,
//   withCredentials: true,
// });

const socketService = null;

export default socketService;
