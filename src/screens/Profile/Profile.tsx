import { ProfileInfo } from 'modules';

import { ProfileStyled } from './styles';

const Profile = () => {
  return (
    <ProfileStyled>
      <ProfileInfo />
    </ProfileStyled>
  );
};

export default Profile;
